{
  "id": "com.notchly.example.weather",
  "name": "Weather",
  "version": "1.0.0",
  "author": "Notchly",
  "description": "Current conditions from Open-Meteo. Shows how a widget uses the network.",
  "entry": "index.html",
  "icon": "cloud.sun",
  "height": 128,
  "refreshInterval": 900,
  "permissions": ["network"],
  "settings": [
    { "key": "place", "type": "string", "label": "Place", "default": "Tbilisi",
      "help": "Any city name. Looked up through Open-Meteo's geocoding API." },
    { "key": "units", "type": "select", "label": "Units", "default": "Celsius",
      "options": ["Celsius", "Fahrenheit"] }
  ]
}
